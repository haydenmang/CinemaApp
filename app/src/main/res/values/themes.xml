<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CinemaApp" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.CinemaApp" parent="Base.Theme.CinemaApp" />

    <!-- Input field premium -->
    <style name="RoundedInput" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusTopStart">16dp</item>
        <item name="boxCornerRadiusTopEnd">16dp</item>
        <item name="boxCornerRadiusBottomStart">16dp</item>
        <item name="boxCornerRadiusBottomEnd">16dp</item>
        <item name="boxStrokeColor">@color/accent_gold</item>
        <item name="boxStrokeWidth">0dp</item>
        <item name="boxStrokeWidthFocused">1dp</item>
        <item name="hintTextColor">#99FFFFFF</item>
        <item name="boxBackgroundColor">#0DFFFFFF</item>
        <item name="android:minHeight">56dp</item>
    </style>
</resources>